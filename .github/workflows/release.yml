name: Release

on: 
    push:
        tags:
            - 'v*'

jobs:
    build:
        - name: Checkout
          uses: actions/checkout@v6

        - name: Release
          uses: softprops/action-gh-release@v3